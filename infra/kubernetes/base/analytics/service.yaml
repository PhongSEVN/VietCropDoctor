apiVersion: v1
kind: Service
metadata:
  name: analytics
  namespace: vietcropdoctor
  labels:
    app: analytics
spec:
  type: ClusterIP
  selector:
    app: analytics
  ports:
    - name: http
      port: 8004
      targetPort: 8004
      protocol: TCP
